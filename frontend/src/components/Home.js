import React from 'react';
import './Home.css';

const Home = () => {
  return (
    <div className="home">
      {/* UI is empty - displaying nothing as per requirements */}
    </div>
  );
};

export default Home;
